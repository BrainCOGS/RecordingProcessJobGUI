name: EnvAutoPipeGUI
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - scipy
    - pandas
    - datajoint
    - phy --pre --upgrade
    - pip
      - git+https://github.com/datajoint/element-interface.git
